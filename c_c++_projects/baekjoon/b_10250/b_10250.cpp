#include <stdio.h>

// number of roon => W
// floor => H
// customer num => N

int main() {
    int T, H, W, N;
    int num_floor, num_room;
    scanf("%d", &T);

    for (int i = 0; i < T; i++) {
        scanf("%d %d %d", &H, &W, &N);

            
    }
}